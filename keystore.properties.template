# Copy to keystore.properties and fill in. Never commit keystore.properties.
# Generate the keystore with the keytool command in docs/release-checklist.md.
# Play App Signing means this is your UPLOAD key, not the final app signing key.
storeFile=keystore/upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
