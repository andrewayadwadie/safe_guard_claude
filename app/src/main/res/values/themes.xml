<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SafeGuard" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>

        <!-- Status bar matches the surface for a clean look -->
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>

        <!-- Window (light theme: dark status-bar icons over the light surface) -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Native launch splash: Haris logo only, centered on white.
         The Android-12 system splash always clips its icon to a circle, so the icon
         drawable insets the logo to fit fully INSIDE that circle: whole logo visible,
         no cropping, no visible circle edge (no icon background color is set).
         windowBackground covers pre-12 devices the same way. -->
    <style name="Theme.Haris.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_inset</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.SafeGuard</item>
    </style>
</resources>
